require 'spec_helper'

describe Mathjax::Yard do
  it 'has a version number' do
    expect(Mathjax::Yard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
